{
  "dependencies": {
    "ethers": "^5.6.9",
    "fs-extra": "^10.1.0",
    "solc": "^0.8.15"
  },
  "scripts": {
    "compile": "yarn solcjs --bin --abi --include-path node-modules/ --base-path . -o . SimpleStorage.sol"
  }
}
